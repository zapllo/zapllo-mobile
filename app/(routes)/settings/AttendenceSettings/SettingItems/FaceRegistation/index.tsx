import { StyleSheet, Text, View } from "react-native";
import React from "react";
import FaceRegistrationScreen from "~/screens/Attendance/Settings/settingItems/FaceRegistrationScreen";

export default function FaceRegistation() {
  return (
   <FaceRegistrationScreen/>
  );
}

const styles = StyleSheet.create({});
